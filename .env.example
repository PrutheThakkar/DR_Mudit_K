# Use the production URL from the n8n Webhook node (not the test URL).
GATSBY_N8N_CONTACT_WEBHOOK_URL=https://your-n8n-domain.example/webhook/contact

# Google Analytics 4 measurement ID (Admin > Data streams > Web).
GATSBY_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# REQUIRED BEFORE GO-LIVE: the real production domain (with https://, no
# trailing slash). Canonical URLs, Open Graph tags, the generated sitemap,
# and robots.txt all derive from this. Set the real value in Netlify's
# Site settings -> Environment variables — it will NOT pick this up from a
# local .env file on Netlify's build server.
GATSBY_WEBSITE_URL=https://www.your-production-domain.com

# Public WPGraphQL endpoint used at Gatsby build time.
WPGRAPHQL_URL=https://drmuditk.studiosentientdemo.com/graphql
WORDPRESS_USERNAME=your-wordpress-username
WORDPRESS_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx
